version: "1"
packages:
  - name: "db"
    path: "./internal/db"
    queries: "./internal/sql/query/"
    schema: "./migrations/"
    engine: "postgresql"
    emit_prepared_queries: true
    emit_interface: true 
    emit_exact_table_names: false
    emit_json_tags: true
